name: Vulnerability

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  Vulnerability_Tests:
    name: "Vulnerability Tests (300)"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Vulnerability Dependencies
        working-directory: ./vulnerability/automation
        run: npm install --legacy-peer-deps

      - name: Run Vulnerability Analysis Suite
        working-directory: ./vulnerability/automation
        run: npm run test

      - name: Upload Vulnerability Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: vulnerability_test_cases
          path: ./vulnerability/automation/Vulnerability_Complete_Test_Report.xlsx
          retention-days: 30
